# OER-FTAcV 项目纠错记录

> 本文记录项目进行中的错误、风险和待验证事项。项目完成后，只把少量跨项目规则提炼到 `/Users/liushiyu/gpt/项目后纠错.md`。

## 当前问题

### 0. 项目文件曾按语言、证据等级和文档用途混放

- 状态：已修复，需持续执行分类规则。
- 原因：早期项目按开发阶段自然增长，代码、背景文档和不同验证等级的结果缺少稳定边界。
- 修复：建立 `code/`、`documents/`、`results/` 分类，并用各级 README 说明入口和新增文件放置规则。
- 补充纠错：分类分支曾遗漏跟踪旧 `python/README.md`，且 Mac 工作区保留旧路径缓存，导致 Finder 仍显示旧目录；旧缓存已移入废纸篓，旧索引已删除。
- 验证：新增“Git 不得跟踪旧源码目录”回归测试；此前 Python 101 项、Web 后端 3 项测试及 C++ bridge 验证均通过。
- 规则：formal、smoke、diagnostics 结果不得混用；本机秘密和 agent 交接不得提交；移动文件后必须更新索引并运行仓库审计。

### 1. 12/32 points-per-cycle 证据不一致

- 状态：已修复，需保持追溯。
- 原因：脚本配置变化未同步进入正式 CSV 和报告。
- 修复：Legion 重算 32 points/cycle 基线并补充 manifest，关联 commit `4d5035b`。
- 规则：正式结果必须记录采样、数据、种子、预算、commit 和环境。

### 2. FT4 曾因错误扫描速率假设被排除

- 状态：已纠正；相位仍需复验。
- 原因：用约 1 V/s 的假设代替 FT4 实测约 0.00390 V/s。
- 结论：FT4 初步分辨率约 25 mV，不因 1 Hz 自动排除。

### 3. 锁相真实相位圆均值丢失虚部

- 状态：已修复，待正式提交。
- 文件：`code/python/scripts/validate_potential_resolved_harmonics.py`
- 原因：复数圆均值在取角度前被转换为 `float`，输出趋于 0 或 π。
- 修复：使用单位相量圆均值，并以 warnings-as-errors 重跑四个数据集。
- 验证：FT2/FT3/FT4/FT8 的 H1–H7 相位均为有限值，不再只取 0 或 π。

### 4. 锁相目标的相位和有效区处理不完整

- 状态：已修复，真实重采样稳定性 Gate A4 已通过。
- 修复：
  - 从实测电位基频估计应用电位参考相位；
  - 模拟使用已知施加电位相位；
  - 统一 `complex = Q - jI` 与 `phase = angle(complex)`；
  - 插值复数包络，不直接插值环绕角度；
  - objective 只比较实验与模拟有效区交集；
  - 修正实验 target 曾用时间轴插值电位网格的问题。
- 验证：5 个新增回归测试完成 RED→GREEN；79 项全量测试通过。
- 压力测试：H1–H7 在 32/64 points-per-cycle、24/40 周期下最大幅值误差 0.29%，最大相位误差 0.0029 rad。
- 正式复验：
  - FT2/FT3/FT4/FT8 使用抗混叠 2×/4×降采样和单端 10% 截断；
  - 112 行完整，60 行进入门评价；
  - 最坏被评价幅值 NRMSE 0.00411，相位 RMSE 0.01810 rad，峰位漂移 0；
  - Gate A4 PASS，但只支持信号稳定性，不支持反演精度结论。

### 5. Phase 2 smoke 被误写为网格收敛 PASS

- 状态：未解决。
- 证据：`grid_convergence.csv` 仅一个 synthetic target、一个 seed、四个网格。
- 影响：128 点只是候选值。
- 下一步：先用固定参数库比较网格 loss，再做三种子反演。

### 6. signed sensitivity 上游仍为无符号距离

- 状态：未解决。
- 文件：`code/python/src/oer_aem/importance.py`
- 原因：plus/minus 扰动分别计算非负距离后取平均，下游无法恢复符号。
- 影响：当前 signed CSV 和 coupling direction 不能用于机理解释。
- 下一步：从源头计算有符号中心差分，并重新生成证据。

### 7. C++ CN 缺少跨参数空间等价性门

- 状态：正式 24 样本门已完成，结果 **FAIL**。
- 已完成：单轨迹收敛和速度测试，关联 commit `bef7bb2`。
- 新发现：C++ 内部稳态 Newton 会产生错误初值；同一参数样本的总电流 NRMSE 因此达到 18.5%。
- 修复：CN 与 LSODA 统一使用 `OERPhysics.calculate_steady_state()` 生成的初值；`cn` 模式禁止静默回退，结果显式记录 backend。
- 网格压力测试：
  - 32 points/cycle 在 H4–H7 失败；
  - 64 points/cycle 在 6 样本中仍有 16 项失败；
  - 128 points/cycle 在 6 样本、32 周期下通过预注册门。
- 可解析性规则：参考谐波强度低于最强通道 2% 时，不评价其相位和相对误差，但保留原始数值；总电流和 DC 仍必须通过。
- 正式结果：
  - 配置为 commit `1becc125311e`、24 samples、256 cycles、128 points/cycle；
  - sample 12、21 的 LSODA 失败，CSV 只有 156 行而非 168 行；
  - 15 项锁相相位超阈值；
  - sample 20 的 DC NRMSE 为 2.61%，超过 1% 门。
- LSODA 诊断与修复：
  - 两个失败均发生在 `t=0`，原始错误为 repeated convergence failures /
    `Unexpected istate`；初值有限、覆盖度守恒，排除 NaN 初值；
  - 显式设置 `first_step=1e-8` 后，Legion 同一 24 样本 24/24 成功、
    0 条警告；该值来自固定样本诊断，不代表已经证明所有参数空间稳定；
  - 回归测试锁定该求解配置，防止后续无记录删除。
- 决策：原 Gate A3 和 CN 禁用决定不变；不调整阈值。下一步以新 commit
  独立测试 256 points/cycle。
- 256 points/cycle 正式复验：
  - commit `a4581dea2d8`，24 samples、256 cycles、256 points/cycle；
  - 168 行结构完整、两求解器全部成功、指标有限、provenance 完整；
  - 仍有 22 项锁相相位误差超阈值，且包含 H2/H3；
  - 因此主要 CN 偏差不是输出采样密度不足。
- 最终处置：停止继续增加 points/cycle；LSODA 冻结为唯一正式后端，
  CN 隔离为实验后端。未来若恢复 CN，先诊断离散方程和相位传播，不得
  通过删样本、改阈值或仅提高采样率宣称等价。

### 7.1 WSL 后台任务被宿主回收

- 状态：已修复并通过 SSH 断连验证。
- 原因：Windows `.wslconfig` 缺失时 `vmIdleTimeout` 默认 60000 ms；宿主侧 WSL 会话退出后，tmux/setsid 任务随发行版回收。
- 证据：Windows 事件日志记录 WSL 虚拟网卡在任务启动约 96 秒后删除；两次后台任务均停在约 sample 6/24 且无退出码。
- 修复：Windows `.wslconfig` 设置 `[wsl2] vmIdleTimeout=86400000`，
  `WSL-Keeper` 计划任务维持 VM；Debian 已启用 systemd、linger 和
  `user@1000.service`。
- 验证：用户级 systemd 断连任务在 SSH 退出后成功生成完成标记。
- 规则：正式长任务使用 `systemd-run --user`，不再依赖 tmux/setsid；
  基础设施失败不得写成科学 FAIL。

### 8. `lockin_only` 实际包含全局 Complex-SNR

- 状态：未解决。
- 影响：无法单独归因电位分辨特征的贡献。
- 下一步：拆分为 `lockin_only` 和 `hybrid`，先验证真正的 lock-in-only。

### 9. 正式命令混用不同工作树

- 状态：Gate A3 新正式门已修复，后续需保持。
- 问题：虚拟环境、脚本和输出目录来自不同工作树。
- 风险：源码、动态库、依赖和结果无法形成唯一证据链。
- 修复：256 points/cycle 门使用 commit 专属 worktree，源码、编译产物和
  输出均在同一根目录；外部虚拟环境解释器的绝对路径与版本写入 manifest。
- 规则：后续正式任务继续记录工作树、解释器、动态库、commit 和输出目录。

## 当前决策

- 暂停 Phase 5 formal run。
- Phase 1R 核心代码已修复；完成真实数据重采样稳定性后再关闭 Gate。
- Gate A3 已关闭为 FAIL；正式后端使用 LSODA，CN 保持实验隔离。
- 重做 Phase 2、Phase 3，再重新定义 Phase 4。
