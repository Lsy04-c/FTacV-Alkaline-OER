{
  "name": "oer-ftacv-workbench",
  "version": "0.2.0",
  "private": true,
  "description": "碱性 OER AEM FTacV 参数反演工作台（FastAPI 后端 + 静态前端，同源部署）",
  "scripts": {
    "dev": "python -m uvicorn backend.main:app --host 0.0.0.0 --port 7100"
  }
}
