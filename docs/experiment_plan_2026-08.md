# 补充实验清单（2026-08）

负责人：刘拾玉
状态：待执行（用户休假中，预计数日后可进实验室）
依据：`docs/superpowers/plans/2026-08-22-low-dim-bonke-model.md`、`docs/项目纠错.md` 第 14-16 条

> A、B 两节不依赖任何模型结果，可直接执行。
> C 节的具体数值由 `results/low_dim_bonke/posterior_summary.csv` 填定。

---

## A. 与模型无关，优先执行

> **A3 是前置阻断项。** 2026-08-22 的 Ru 敏感性实测表明，在 Ru 未测定前
> 反演出的 `E0_eff` / `k0` / `gamma` 绝对值没有意义。其余实验可与 A3 并行，
> 但正式反演必须等 A3。

| # | 实验 | 破解的问题 | 成本 |
|---|---|---|---|
| A1 | **空白基底 FTacV**：同基底、同电解液、同参数，无催化剂 | 基底背景电流未扣除——项目自己判定的欠拟合根因之一 | 1 次测量 |
| A2 | **同一电极重复 3 次 FTacV** | 重复性权重的唯一合法来源。roadmap §2.3 明确禁止把单次信号的局部 SNR 当实验不确定度 | ~30 min |
| **A3** | **EIS 测 Ru —— 前置阻断项，必须最先做** | 实测（纠错 §15）：Ru 由 5 改到 20 Ω 时 E⁰_eff 移动 154–349 mV、k₀ 变 8–137 倍、γ 变 0.3–35 倍，而拟合质量几乎不变。**数据区分不出 Ru，但 Ru 决定机理参数。** 在测出 Ru 之前，任何机理参数的绝对值都不可解释 | ~10 min |
| A4 | **补记录元数据** | 参比电极、电解液浓度、催化剂、负载量、电极真实面积。缺参比则 `E0_eff` 绝对值不可用（纠错 §16） | 0 |

## B. 最高杠杆：降低负载 + 收紧电流量程

四组现有数据的噪声底由 ADC 量化台阶 `q = 3.0e-5 A` 决定
（`q/sqrt(12) = 8.7 µA`，与实测噪声底在一位有效数字内相等），
H4-H7 信噪比只有 0.7-5.7。

Bonke 2016 能在 CoOx 上完成水氧化 FTacV 反演的前提是
**Γ = 3-110 pmol/cm² 的超低负载**——催化直流电流足够小，谐波才被表面
氧化还原主导。本项目假定的 `gamma = 3e-9 mol/cm²` 高出 30-1000 倍，
峰值电流 11 mA，正处于 Bonke 刻意避开的区间。

两者是连锁的：

```
负载降 100x  ->  峰值电流 ~100 µA  ->  电流量程可加严 ~100x
             ->  量化台阶 ~3e-7 A   ->  H4-H7 SNR 从 1-5 变成 100-500
```

| # | 实验 | 说明 |
|---|---|---|
| B1 | **负载梯度**，4 个点跨 2 个数量级，向 pmol/cm² 量级靠 | 同时给出 Bonke 的 j-vs-Γ 曲线；并让 `gamma` 与 `A` 的简并可由外部信息打破 |
| B2 | **每个负载点收紧 CHI 电流量程** | 量化台阶随量程按比例下降。这是本清单中收益最高、成本最低的一条 |
| B3 | **一组 ΔE = 80 mV 对照**（现为 160 mV） | Bond 组标准值。160 mV 谐波更丰富但 iRu 畸变与法拉第整流也更强 |
| B4 | **保留一组完全按旧参数** | 保证与现有四组的可比性，避免整批数据断代 |

## C. 需要低维模型后验才能定的实验

| # | 实验 | 由哪个后验量决定 |
|---|---|---|
| C1 | **频率组** | `k0` 后验宽度。Bonke 试过 9 / 22 / 89 Hz，结论是 9 Hz 已足够、更高频无增益——不要盲扫。现有 FT3(5 Hz)/FT4(1 Hz) 在同一窗口给出 H3 峰位 +40 mV 位移，可作为区间起点 |
| C2 | **电位窗口与扫速** | `E0_eff` 后验位置与是否双峰。若确认特征在 1.54 / 1.60 V，新窗口可收到 1.20-1.70 V，扫得更慢、点更密，完全避开高电流区 |

待填（运行结束后从 `results/low_dim_bonke/posterior_summary.csv` 抄入）：

```text
k0    后验中位数 / 95% 区间：____
kf    后验中位数 / 95% 区间：____
gamma 后验中位数 / 95% 区间：____
E0_eff 后验中位数 / 95% 区间：____
最强参数相关对（见 correlation.csv）：____
=> 建议频率点：____
=> 建议电位窗口 / 扫速：____
```

## D. 一并确认的问题

- **FT3 与 FT4 是否同一电极？**（纠错 §18）是则已在手一组两点频率色散。
